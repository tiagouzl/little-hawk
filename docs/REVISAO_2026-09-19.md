# Revisão — Trilha RWKV-7, Benchmarks e Spec Tiny-Runtime 🦅

> **Data:** 19/09/2026 · **Escopo:** `engine/rwkv_*`, `benchmarks/transformer_vs_rwkv/`, `files/chat_baseline*`, `docs/superpowers/specs/2026-09-19-tiny-runtime-design.md`
> **Método:** leitura de código + dados de `results_*.json` + `ruff check` nos arquivos novos. Nenhum benchmark re-executado.

---

## Sumário

- [1. Veredito](#1-veredito)
- [2. O que está bom](#2-o-que-está-bom)
- [3. Bugs e riscos](#3-bugs-e-riscos)
- [4. Revisão do spec tiny-runtime](#4-revisão-do-spec-tiny-runtime)
- [5. Ideias de aprimoramento](#5-ideias-de-aprimoramento)
- [6. Resumo](#6-resumo)

---

## 1. Veredito

O trabalho RWKV está **bem feito e bem documentado**. Os riscos reais são:

1. `.pth` de 365 MB prestes a entrar no git;
2. contradição física no spec (orçamento 1024 vs janela 512 do engine);
3. prefill sequencial do RWKV (~26× mais lento).

A melhoria de maior impacto continua sendo a receita que já funcionou no transformer (lição §11.2 do relatório 25/08): **mudar a forma, não acelerar o passo** — agora na recorrência, via prefill chunked.

---

## 2. O que está bom

- **Honestidade epistêmica exemplar.** `benchmarks/transformer_vs_rwkv/README.md` separa "medido" de "pendente", documenta o confundidor do ChatML, marca a sessão contaminada por swap e recusa-se a fechar Q8/Q10 sem bateria limpa. Este é o maior patrimônio do projeto.
- **`engine/rwkv_engine.py` limpo e correto.** Fórmulas batem com a referência oficial (equivalência top1 ✓, mediana rel 0.011, tolerâncias explícitas em `tests/test_rwkv.py`). Transposição seletiva dos `nn.Linear` (pares LoRA e vetores `x_*` fora da lista `_T`) documentada no local exato. Estado O(1) auditável via `state_bytes()`.
- **Protocolo de benchmark correto.** A/B intercalado por `(ctx, rep)` + `reset_peak()` via `/proc/self/clear_refs` — metodologia certa para thermal throttling.
- **`files/chat_baseline.py` é um scorer sério.** IC95% Wilson por categoria, AUROC dos sinais de confiança (`mean_logprob`, `margin_top1_top2`), selftest que valida o próprio scorer contra gold e negativos (vazio/degenerado).
- **Separação de responsabilidades.** Conversão torch→npz offline em `scripts/convert_rwkv.py`; runtime 100% NumPy; `engine/` intacto.

---

## 3. Bugs e riscos

### 3.1 CRÍTICO — `.pth` de 365 MB vai parar no git

`.gitignore` cobre `*.npz` mas **não** `data/rwkv/*.pth` — e `git status` mostra `?? data/rwkv/`. Um `git add .` comete 365 MB de binário.

```gitignore
# adicionar ao .gitignore
data/rwkv/*.pth
```

(Vocab, meta e SHA256.txt podem ser versionados — são pequenos.)

### 3.2 Typo-armadilha em `engine/rwkv_engine.py:54`

```python
self.n_embd = n_layer and n_embd  # → 768 hoje; 0 se n_layer=0
```

Funciona por acidente (`n_layer` sempre truthy no load real). Corrigir para `self.n_embd = n_embd`.

### 3.3 CI vai quebrar no lint

`ruff check` nos arquivos novos = **20 erros** (4 auto-fixáveis): C401/C416 em `files/chat_baseline.py`, EXE001 em `scripts/convert_rwkv.py`, entre outros. O relatório de 25/08 afirma "ruff check ✅", mas isso vale para o estado anterior aos arquivos novos.

```bash
venv/bin/python -m ruff check --fix engine/rwkv_engine.py engine/rwkv_tokenizer.py \
  benchmarks/transformer_vs_rwkv/ files/ scripts/convert_rwkv.py
```

### 3.4 `files/lh_adapter.py` trunca a **cabeça** do prompt ChatML

```python
if len(ids) > maxctx - max_new_tokens:
    ids = ids[-(maxctx - max_new_tokens) :]
```

Manter o tail descarta `<|im_start|>system` inicial → template malformado exatamente quando o prompt é longo. Truncar o **meio** (head + tail), preservando system e a pergunta final.

### 3.5 `memory_probe.py` caso B não testa evicção

`B_hist_evict` tem input idêntico ao A com ~30 tokens de contexto — a janela FIFO de 508 **nunca satura**, então A≡B por construção e o resultado do B não mede evicção. Preencher com distratores até o prompt passar de 512 tokens.

### 3.6 Needle com `--gen 12` é curto demais

Saídas capturadas (`'Resposta: O identificador secreto é "12…'`) mostram o orçamento estourando **antes** do número ser emitido. Com gen=24–32, a taxa de hit deixa de ser subestimada por truncamento.

### 3.7 `_sigmoid` sem proteção de overflow (`rwkv_engine.py:7`)

`np.exp(-x)` solta `RuntimeWarning: overflow` e vira `inf` em entradas negativas grandes (a recorrência amplifica). O resultado numérico está correto (`1/(1+inf)=0`), mas o warning polui runs longos. Estabilizar ou isolar com `np.errstate`.

### 3.8 README do benchmark cita números obsoletos

`results_rw_iso.json` / `results_tf_iso.json` (sessão isolada) já existem:

| ctx | prefill transformer (iso) | prefill RWKV (iso) |
| --: | ---: | ---: |
| 128 | 1.151 ms | 36.294 ms |
| 512 | 5.810 ms | 141.380 ms |

O README ainda apresenta os valores contaminados (1.717/6063 ms e 44.404/168.755 ms) como os atuais. Gap real de prefill: **~24–31×**.

---

## 4. Revisão do spec tiny-runtime

O design (`docs/superpowers/specs/2026-09-19-tiny-runtime-design.md`) é coerente e bem delimitado (YAGNI correto, self-review honesto), mas tem 5 furos:

### 4.1 Contradição física: orçamento 1024 vs janela 512

`cli/main.py:258` hardcodda `window_size=508` (`max_cap=512`) e `engine/engine.py:185` trunca prefill além disso. Com contexto >512, freeze + FIFO **descarta silenciosamente** turnos antigos — o "hard cap 1024" do spec não existe no engine. Saídas:

- (a) alinhar o orçamento do spec a 512; ou
- (b) **(melhor)** expor `--window-size` no CLI: para Qwen2.5-0.5B (24L, 2 KV heads), 512→1024 custa só ~25 MB de KV extra.

### 4.2 Modelo não especificado o suficiente

"Qwen engine" precisa dizer **`Qwen2.5-0.5B-Instruct` + template ChatML**. O próprio `res_135m.json` (2/40, chitchat 100% degenerado) prova que base-model sem template não serve para chat; o RWKV só venceu o memory probe por tolerar continuação pura.

### 4.3 Extração de `key` não definida

"lembre que a reunião é na terça" → qual key no schema `facts(key, value, ...)`? É o componente mais frágil do router e o spec não diz como derivar. Sugestão: guardar texto cru + ts e deixar o FTS5 fazer o match; `key` estruturada apenas para padrões explícitos ("meu projeto é X" → `project_name`).

### 4.4 `len//4` subestima tokens em PT

Palavras acentuadas viram ~3 chars/token no BPE do Qwen. O tokenizer já está em processo — contagem exata é gratuita. Alternativa mínima: rodar `chat_baseline` no Qwen **antes** de construir o runtime, com gate quantitativo (ex.: ≥20/40).

### 4.5 Lacunas menores

- Histórico entre sessões: a tabela `messages` existe — replay dos últimos N turnos no boot?
- Pergunta do usuário >50 tokens (orçamento "pergunta 50").
- FTS5 sem `tokenize='unicode61 remove_diacritics 2'` não casa "memória" com "memoria".

---

## 5. Ideias de aprimoramento

### 5.1 Quick wins (horas)

| # | Ideia | Ganho |
|---|---|---|
| 1 | `.gitignore` + `data/rwkv/*.pth`; `ruff check --fix` nos novos | CI verde, repo seguro |
| 2 | `--window-size` no CLI | Resolve a contradição 1024 do spec por ~25 MB |
| 3 | Atualizar README do benchmark com resultados iso | Números honestos (gap 24–31×) |
| 4 | Truncamento meio-fora/meio-dentro no `lh_adapter` | Corrige degradação silenciosa |
| 5 | gen=24 no needle + contexto >512 no memory probe | Medição de evicção real |

### 5.2 Estruturais

**A. Prefill chunked para RWKV — maior impacto de todos.** O gap de 26× é o mesmo problema já resolvido no transformer: a recorrência por token faz einsum `(H,64,64)` por passo (~280 ms/token). A formulação chunked da atenção linear (blocos de 32–64 tokens; intra-chunk via GEMM; entre-chunks via estado) transforma isso em BLAS de verdade. É o análogo exato do prefill batched que deu 10–20×. Com isso o RWKV deixa de ser inviável para prompts longos — que é justamente o caso do tiny-runtime (histórico de 500 turnos).

**B. Tokenizer RWKV O(V) por posição** (`rwkv_tokenizer.py:33-40`). O loop varre ~65k tokens ordenados por comprimento para cada posição. Com vocab byte-level basta tentar prefixos do comprimento máximo para baixo com lookup em dict (~60 lookups/posição) — ordens de grandeza mais rápido no encode de histórias longas, zero mudança de comportamento.

**C. Sinal intermediário no needle: rank do token-agulha.** Hoje existe hit binário + texto bruto. Um terceiro sinal barato: logprob da string agulha (soma token a token) no passo da pergunta. Distingue "sabe mas não verbaliza" de "não recuperou" — exatamente a ambiguidade que travou o Q8.

**D. tiny-runtime: slash commands + regex como fallback.** `/lembrar X`, `/abrir Y` custam ~10 linhas no router e eliminam a classe de falsos positivos do regex puro ("meu projeto é um jogo" viraria SAVE_FACT por engano). Logar a decisão do router no `memory.db` permite auditar sem LLM no caminho quente.

**E. Pin de qualidade antes de construir.** Rodar `chat_baseline` no Qwen2.5-0.5B-Instruct (adapter novo, ~40 linhas espelhando `lh_adapter.py`) e fixar o score como baseline do spec. Se o Qwen também degenerar em chitchat PT, o problema é de checkpoint e nenhuma engenharia de memória resolve — melhor saber na semana 0.

### 5.3 Visão de produto (v2+)

- O needle mostrou que **nenhum dos dois** checkpoints recupera HAWK-7319@600 — o gargalo real do produto tiny é o **checkpoint**, não a arquitetura de evicção. Maior ROI: transplantar Qwen2.5-0.5B-Instruct (já suportado) ou incluir `SmolLM2-360M-Instruct` (o `.npz` já existe no repo raiz) como braço intermediário no needle.
- A limitação conhecida do salience ("a reunião é na terça-feira" não gera surpresa) tem patch barato no tiny-runtime: o bloco `Context:` injetado já é o piso de proteção. Formalizar no spec que **fatos salvos são pinados fora do FIFO**, tornando a evicção irrelevante para fatos — que é o que C_injected/D_retrieved já demonstraram no RWKV.

---

## 6. Resumo

| Dimensão | Estado |
|---|---|
| Código RWKV | ✅ correto (equivalência oficial), 1 typo latente |
| Benchmarks | ✅ metodologia sólida; ❌ números contaminados no README, caso B sem evicção, gen curto |
| Higiene | ❌ `.pth` 365 MB no git-add pendente; ❌ 20 erros de ruff |
| Spec tiny-runtime | ⚠️ bom escopo; ❌ 1024-vs-512, modelo/template indefinidos, extração de `key` indefinida |
| Próximo passo de maior ROI | Prefill chunked RWKV + baseline de qualidade do Qwen-Instruct como gate do spec |

---

*Revisão gerada por Codebuff em 19/09/2026. Para reproduzir as checagens: `git status --short`, `venv/bin/python -m ruff check engine/rwkv_engine.py engine/rwkv_tokenizer.py benchmarks/transformer_vs_rwkv/ files/ scripts/convert_rwkv.py`, `venv/bin/python -m pytest tests/test_rwkv.py -q`.*
