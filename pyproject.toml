[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "little-hawk"
version = "0.8.0"
description = "LLM streaming engine em Python/NumPy puro"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "tiagouzl" }]
keywords = ["llm", "inference", "numpy", "streaming", "transformer"]
dependencies = [
    "numpy",
    "safetensors",
    "huggingface_hub",
    "tokenizers",
]

[project.optional-dependencies]
# Servidor FastAPI — o motor (engine/, runtime/) não importa nada daqui;
# "Sem frameworks" vale literalmente para `infer`/`transplant`
api = ["fastapi", "uvicorn[standard]"]
dev = ["pytest", "ruff", "httpx<0.28", "fastapi", "uvicorn[standard]"]
jit = ["numba>=0.58.0"]
onnx = ["torch", "onnx", "onnxruntime"]
# cython removido em 2026-08-24 — POC rejeitado (0.70×, §13-14 ANALISE.md); .pyx mantido como referência histórica
# Para o teste de equivalência numérica (tests/test_equivalence.py):
equiv = [
    "torch; platform_system != 'Windows'",
    "transformers",
]

[project.urls]
Repository = "https://github.com/tiagouzl/little-hawk"

[tool.setuptools]
packages = ["api", "cli", "engine", "runtime", "transplants", "utils"]

[tool.pytest.ini_options]
testpaths = ["tests"]
