# Fonte da verdade: pyproject.toml — este arquivo é compat para CI/Docker
# Core de inferência (sem frameworks):
numpy
safetensors
huggingface_hub
tokenizers
# Bloco API — necessário aqui porque o CI roda test_api.py e o Docker serve
# uvicorn api.server:app. Para instalar só o motor: `pip install -e .`
fastapi
uvicorn[standard]
# Dev (testes/lint):
pytest
ruff
httpx<0.28
